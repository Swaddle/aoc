use std::collections::HashMap;

pub struct Robot {
    pub x: i32, 
    pub y: i32,
}

pub fn free_space(grid: &HashMap<(i32, i32), char>, x: i32, y: i32) -> bool {
    return true;
}

pub fn p1() -> u64 {

    // hashmap of coord to box/wall/empty space
    // box = "O'
    // wall = "#"
    // empty space = "."
    let mut grid = HashMap::new();

}



